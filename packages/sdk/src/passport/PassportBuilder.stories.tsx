import {Meta, StoryObj} from "@storybook/react";
import {PassportBuilder} from "./PassportBuilder";

export default {
  component: PassportBuilder,
  title: "PassportBuilder",
} as Meta<typeof PassportBuilder>;

type Story = StoryObj<typeof PassportBuilder>;

export const Default: Story = {};
